% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-source.R
\name{get_audit_data}
\alias{get_audit_data}
\title{Get audit data from configured source}
\usage{
get_audit_data(config)
}
\arguments{
\item{config}{A \code{dashboard_config} object from \code{\link[=dashboard_config]{dashboard_config()}}.}
}
\value{
A tibble with one row per patient and columns:
\code{record_id}, \code{centre_code}, \code{centre_name}, \code{consultant}, \code{age},
\code{gender}, \code{dxs01}, \code{qblg9} (URR), \code{hdp01} (HD freq), \code{hdp02} (HD
duration), \code{qblg3}/\code{qblg4} (pre BP), \code{qblg6}/\code{qblg7} (post BP),
\code{qblb1} (PO4), \code{qblb4} (Ca), \code{qblb9} (PTH), \code{qbla9} (K),
\code{qbla4} (HCO3), \code{qble1} (Hb), \code{qblf1} (ferritin), \code{qhd20} (access).
}
\description{
Factory function that returns audit data from either the synthetic
generator or REDCap, depending on the \code{data_source} setting in config.
Both sources return identically structured tibbles.
}
\examples{
config <- dashboard_config()
df <- get_audit_data(config)
nrow(df)
}
\seealso{
Other data: 
\code{\link{cached_audit_data}()},
\code{\link{generate_synthetic_data}()}
}
\concept{data}
